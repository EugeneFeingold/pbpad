"""Configuration for pbpad.

`conf.config` holds the app's tunables and pin map (imported everywhere as
`from conf import config`). The `*.conf.example` templates and the git-ignored
`deploy.conf` live here too, so all configuration is in one place.
"""
